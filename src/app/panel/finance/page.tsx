/**
 * The staff Financial Work Book — the same component as the member's, for the
 * same reason as `panel/operations/page.tsx`: the book a row lands in is
 * decided by the author's role on the server, not by which screen was used.
 */
export { default } from "@/app/dashboard/finance/page";
