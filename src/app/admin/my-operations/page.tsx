/**
 * Admin's personal Daily Operation Book — same component as member/panel DOB,
 * kept under /admin so the admin shell layout is not left for /panel.
 */
export { default } from "@/app/dashboard/operations/page";
