/**
 * Admin's own "new FWB invoice" screen — same component, kept under /admin so
 * the admin shell layout is not left for /dashboard or /panel.
 */
export { default } from "@/app/dashboard/finance/new/page";
