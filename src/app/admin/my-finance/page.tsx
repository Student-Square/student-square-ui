/**
 * Admin's personal Financial Work Book — same component as member/panel FWB,
 * kept under /admin so the admin shell layout is not left for /panel.
 */
export { default } from "@/app/dashboard/finance/page";
